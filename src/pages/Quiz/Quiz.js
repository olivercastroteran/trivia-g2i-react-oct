import React from 'react';
import Spinner from '../../components/Spinner/Spinner';
import './Quiz.scss';

const Quiz = () => {
  return (
    <div className="quiz">
      <h1>Quiz</h1>
      <Spinner />
    </div>
  );
};

export default Quiz;
